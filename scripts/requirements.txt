# The model generators' toolchain, PINNED — see .github/workflows/build.yml's
# model-selfcheck job, which rebuilds every .glb and then fails if the result
# differs from the committed bytes. That check compares FLOAT ARRAYS a
# trimesh release is free to shift by an ulp, so an unpinned install would turn
# a green gate into a coin flip on somebody else's release day. Pinned, the
# comparison means what it says: the committed artifact is stale.
#
# Bump these deliberately, rebuild the models in the same commit, and expect a
# .glb diff to come with the bump.
trimesh==5.0.0
numpy==2.5.2

# SHAPELY AND MAPBOX-EARCUT ARE GONE, and this note is their epitaph so nobody
# adds them back by reflex. They were here for exactly one call,
# `trimesh.creation.extrude_polygon` (bead godot-test1-y1o.21): it wants a
# shapely polygon and a triangulation engine, and trimesh ships neither. Two
# heroes used it — Primm's extruded cyan chest trim, retired with his parts by
# bead godot-test1-5u3.6, and Windman's extruded chest "W", retired with his by
# bead 5u3.5, both of them vertex colour on a skinned mesh now. Nothing in
# `scripts/` imports either package any more (`grep -rn 'shapely\|earcut'
# scripts/*.py` answers only in prose), and `predator_parts.py` was always
# runnable without them by design. The next caller brings its own pin back, with
# the same reasoning written down: a triangulator is free to emit the same
# polygon as a different fan, which is a geometry diff and not an ulp, so the
# rebuild-and-diff gate needs it pinned or it is a coin flip.
