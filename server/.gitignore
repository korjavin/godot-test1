# go build ./... drops the binary here, named after the directory
/server
lobby

# Local compose env. docker-compose.yml reads .env by default and .env.example
# names TURN_SECRET / TURN_PASSWORD — a shared secret for a coturn running on
# the host network is not a thing to publish to a public repo by accident.
.env
.env.*
!.env.example
